const styles = `
    header {
        /* Se precisar de algum estilo específico para o contêiner, ele viria aqui. */
        /* Por exemplo: */
        display: block;
        position: relative;
        z-index: 999; /* Garante que o header fique acima de outro conteúdo se necessário */
    }
`;

export default styles;